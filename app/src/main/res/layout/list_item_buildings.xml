<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:descendantFocusability="blocksDescendants">

    <ImageView
        android:layout_width="65dp"
        android:layout_height="65dp"
        android:id="@+id/list_item_building"
        android:src="@drawable/ic_menu_gallery"
        android:layout_alignParentTop="true"
        android:layout_toLeftOf="@+id/list_item_text_f"
        android:layout_toStartOf="@+id/list_item_text_f" />

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Building"
        android:id="@+id/list_item_text_building"
        android:layout_alignBottom="@+id/list_item_building"
        android:layout_centerHorizontal="true" />

</RelativeLayout>